#include <stdio.h>
#include <math.h>

const double PI = 3.1415926535;
const double degreesToRadianConst = 1 / 180.0;

int main() {
  /* double x = 2.9; */

  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 2.95; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 3.0; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 3.05; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 3.1; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 3.15; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 3.2; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 3.25; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 3.3; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 3.35; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 3.4; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 3.45; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 3.5; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 3.55; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 3.6; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 3.65; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 3.7; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 3.75; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 3.8; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 3.85; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 3.9; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 3.95; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 4.0; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* double x = 4.05; */

  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 4.1; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 4.15; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 4.2; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 4.25; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 4.3; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 4.35; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 4.4; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 4.45; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 4.5; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 4.55; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 4.6; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 4.65; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 4.7; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 4.75; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 4.8; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 4.85; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 4.9; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 4.95; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* x = 5.0; */
  /* printf("2.0 * (%.3f) / (4.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 2.0 * x / (4.0 + x * x)); */

  /* double x = 4.05; */

  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 4.1; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 4.15; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 4.2; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 4.25; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 4.3; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 4.35; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 4.4; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 4.45; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 4.5; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 4.55; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 4.6; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 4.65; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 4.7; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 4.75; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 4.8; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 4.85; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 4.9; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 4.95; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 5.0; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* double x = 4.05; */

  /* printf("0.5 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 4.1; */
  /* printf("0.5 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 4.15; */
  /* printf("0.5 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 4.2; */
  /* printf("0.5 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 4.25; */
  /* printf("0.5 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 4.3; */
  /* printf("0.5 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 4.35; */
  /* printf("0.5 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 4.4; */
  /* printf("0.5 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 4.45; */
  /* printf("0.5 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 4.5; */
  /* printf("0.5 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 4.55; */
  /* printf("0.5 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 4.6; */
  /* printf("0.5 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 4.65; */
  /* printf("0.5 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 4.7; */
  /* printf("0.5 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 4.75; */
  /* printf("0.5 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 4.8; */
  /* printf("0.5 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 4.85; */
  /* printf("0.5 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 4.9; */
  /* printf("0.5 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 4.95; */
  /* printf("0.5 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 5.0; */
  /* printf("0.5 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* double x = 4.05; */

  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 4.1; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 4.15; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 4.2; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 4.25; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 4.3; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 4.35; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 4.4; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 4.45; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 4.5; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 4.55; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 4.6; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 4.65; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 4.7; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 4.75; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 4.8; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 4.85; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 4.9; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 4.95; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 5.0; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* double angle = 89.0; */
  /* double x = cos(PI * angle * degreesToRadianConst); */
  /* double y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 4.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 88.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 4.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  double angle = 87.0;
  double x = cos(PI * angle * degreesToRadianConst);
  double y = sin(PI * angle * degreesToRadianConst);

  printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y);

  x = 4.0 - x;
  printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y);

  angle = 86.0;
  x = cos(PI * angle * degreesToRadianConst);
  y = sin(PI * angle * degreesToRadianConst);

  printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y);

  x = 4.0 - x;
  printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y);

  angle = 85.0;
  x = cos(PI * angle * degreesToRadianConst);
  y = sin(PI * angle * degreesToRadianConst);

  printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y);

  x = 4.0 - x;
  printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y);

  angle = 84.0;
  x = cos(PI * angle * degreesToRadianConst);
  y = sin(PI * angle * degreesToRadianConst);

  printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y);

  x = 4.0 - x;
  printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y);

  angle = 83.0;
  x = cos(PI * angle * degreesToRadianConst);
  y = sin(PI * angle * degreesToRadianConst);

  printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y);

  x = 4.0 - x;
  printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y);

  angle = 82.0;
  x = cos(PI * angle * degreesToRadianConst);
  y = sin(PI * angle * degreesToRadianConst);

  printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y);

  x = 4.0 - x;
  printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y);

  angle = 81.0;
  x = cos(PI * angle * degreesToRadianConst);
  y = sin(PI * angle * degreesToRadianConst);

  printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y);

  x = 4.0 - x;
  printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y);

  angle = 80.0;
  x = cos(PI * angle * degreesToRadianConst);
  y = sin(PI * angle * degreesToRadianConst);

  printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y);

  x = 4.0 - x;
  printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y);





  return 0;
}
