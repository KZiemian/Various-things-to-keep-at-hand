#include <stdio.h>
#include <math.h>

int main() {
  /* double x = -0.95; */

  /* printf("2.0 * sqrt(1 - 0.111111 * (%.3f)^2) = %.4f.\n", x, */
  /* 	 2.0 * sqrt(1 - 0.111111 * x * x)); */

  /* x = -0.9; */
  /* printf("2.0 * sqrt(1 - 0.111111 * (%.3f)^2) = %.4f.\n", x, */
  /* 	 2.0 * sqrt(1 - 0.111111 * x * x)); */

  /* x = -0.85; */
  /* printf("2.0 * sqrt(1 - 0.111111 * (%.3f)^2) = %.4f.\n", x, */
  /* 	 2.0 * sqrt(1 - 0.111111 * x * x)); */

  /* x = -0.8; */
  /* printf("2.0 * sqrt(1 - 0.111111 * (%.3f)^2) = %.4f.\n", x, */
  /* 	 2.0 * sqrt(1 - 0.111111 * x * x)); */

  /* x = -0.75; */
  /* printf("2.0 * sqrt(1 - 0.111111 * (%.3f)^2) = %.4f.\n", x, */
  /* 	 2.0 * sqrt(1 - 0.111111 * x * x)); */

  /* x = -0.7; */
  /* printf("2.0 * sqrt(1 - 0.111111 * (%.3f)^2) = %.4f.\n", x, */
  /* 	 2.0 * sqrt(1 - 0.111111 * x * x)); */

  /* x = -0.65; */
  /* printf("2.0 * sqrt(1 - 0.111111 * (%.3f)^2) = %.4f.\n", x, */
  /* 	 2.0 * sqrt(1 - 0.111111 * x * x)); */

  /* x = -0.6; */
  /* printf("2.0 * sqrt(1 - 0.111111 * (%.3f)^2) = %.4f.\n", x, */
  /* 	 2.0 * sqrt(1 - 0.111111 * x * x)); */

  /* x = -0.55; */
  /* printf("2.0 * sqrt(1 - 0.111111 * (%.3f)^2) = %.4f.\n", x, */
  /* 	 2.0 * sqrt(1 - 0.111111 * x * x)); */

  /* x = -0.5; */
  /* printf("2.0 * sqrt(1 - 0.111111 * (%.3f)^2) = %.4f.\n", x, */
  /* 	 2.0 * sqrt(1 - 0.111111 * x * x)); */

  /* x = -0.45; */
  /* printf("2.0 * sqrt(1 - 0.111111 * (%.3f)^2) = %.4f.\n", x, */
  /* 	 2.0 * sqrt(1 - 0.111111 * x * x)); */

  /* x = -0.4; */
  /* printf("2.0 * sqrt(1 - 0.111111 * (%.3f)^2) = %.4f.\n", x, */
  /* 	 2.0 * sqrt(1 - 0.111111 * x * x)); */

  /* x = -0.35; */
  /* printf("2.0 * sqrt(1 - 0.111111 * (%.3f)^2) = %.4f.\n", x, */
  /* 	 2.0 * sqrt(1 - 0.111111 * x * x)); */

  /* x = -0.3; */
  /* printf("2.0 * sqrt(1 - 0.111111 * (%.3f)^2) = %.4f.\n", x, */
  /* 	 2.0 * sqrt(1 - 0.111111 * x * x)); */

  /* x = -0.25; */
  /* printf("2.0 * sqrt(1 - 0.111111 * (%.3f)^2) = %.4f.\n", x, */
  /* 	 2.0 * sqrt(1 - 0.111111 * x * x)); */

  /* x = -0.2; */
  /* printf("2.0 * sqrt(1 - 0.111111 * (%.3f)^2) = %.4f.\n", x, */
  /* 	 2.0 * sqrt(1 - 0.111111 * x * x)); */

  double x = -0.15;

  printf("2.0 * sqrt(1 - 0.111111 * (%.3f)^2) = %.4f.\n", x,
	 2.0 * sqrt(1 - 0.111111 * x * x));

  x = -0.1;
  printf("2.0 * sqrt(1 - 0.111111 * (%.3f)^2) = %.4f.\n", x,
	 2.0 * sqrt(1 - 0.111111 * x * x));

  x = -0.05;
  printf("2.0 * sqrt(1 - 0.111111 * (%.3f)^2) = %.4f.\n", x,
	 2.0 * sqrt(1 - 0.111111 * x * x));

  x = 0.0;
  printf("2.0 * sqrt(1 - 0.111111 * (%.3f)^2) = %.4f.\n", x,
	 2.0 * sqrt(1 - 0.111111 * x * x));
































































  return 0;
}
