#include <stdio.h>
#include <math.h>

const double PI = 3.1415926535;
const double degreesToRadianConst = 1 / 180.0;

int main() {
  /* double angle = 88.0; */
  /* double x = cos(PI * angle * degreesToRadianConst); */
  /* double y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* angle = 87.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* angle = 86.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* angle = 85.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* angle = 84.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* angle = 83.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* angle = 82.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* angle = 81.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* angle = 80.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* printf("(0.034)^2 + (0.999)^2 = %4.f", */
  /* 	 0.034 * 0.034 + 0.999 * 0.999); */


  /* double angle = 79.0; */
  /* double x = cos(PI * angle * degreesToRadianConst); */
  /* double y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 78.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 77.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 76.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 75.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 74.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 73.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 72.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 71.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 70.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* double angle = 69.0; */
  /* double x = cos(PI * angle * degreesToRadianConst); */
  /* double y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 68.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 67.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 66.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 65.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 64.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 63.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 62.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 61.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 60.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* double angle = 59.0; */
  /* double x = cos(PI * angle * degreesToRadianConst); */
  /* double y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 58.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 57.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 56.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 55.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 54.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 53.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 52.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 51.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 50.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* double angle = 49.0; */
  /* double x = cos(PI * angle * degreesToRadianConst); */
  /* double y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 50.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 48.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 47.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 46.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 45.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 44.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 43.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 42.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 41.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  /* angle = 40.0; */
  /* x = cos(PI * angle * degreesToRadianConst); */
  /* y = sin(PI * angle * degreesToRadianConst); */

  /* printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y); */

  /* x = 3.0 - x; */
  /* printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y); */

  double angle = 39.0;
  double x = cos(PI * angle * degreesToRadianConst);
  double y = sin(PI * angle * degreesToRadianConst);

  printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y);

  x = 3.0 - x;
  printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y);

  angle = 38.0;
  x = cos(PI * angle * degreesToRadianConst);
  y = sin(PI * angle * degreesToRadianConst);

  printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y);

  x = 3.0 - x;
  printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y);

  angle = 37.0;
  x = cos(PI * angle * degreesToRadianConst);
  y = sin(PI * angle * degreesToRadianConst);

  printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y);

  x = 3.0 - x;
  printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y);

  angle = 36.0;
  x = cos(PI * angle * degreesToRadianConst);
  y = sin(PI * angle * degreesToRadianConst);

  printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y);

  x = 3.0 - x;
  printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y);

  angle = 35.0;
  x = cos(PI * angle * degreesToRadianConst);
  y = sin(PI * angle * degreesToRadianConst);

  printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y);

  x = 3.0 - x;
  printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y);

  angle = 34.0;
  x = cos(PI * angle * degreesToRadianConst);
  y = sin(PI * angle * degreesToRadianConst);

  printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y);

  x = 3.0 - x;
  printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y);

  angle = 33.0;
  x = cos(PI * angle * degreesToRadianConst);
  y = sin(PI * angle * degreesToRadianConst);

  printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y);

  x = 3.0 - x;
  printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y);

  angle = 32.0;
  x = cos(PI * angle * degreesToRadianConst);
  y = sin(PI * angle * degreesToRadianConst);

  printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y);

  x = 3.0 - x;
  printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y);

  angle = 31.0;
  x = cos(PI * angle * degreesToRadianConst);
  y = sin(PI * angle * degreesToRadianConst);

  printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y);

  x = 3.0 - x;
  printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y);

  angle = 30.0;
  x = cos(PI * angle * degreesToRadianConst);
  y = sin(PI * angle * degreesToRadianConst);

  printf("Pierwszy punkt kontrolny: (%.4f, %.4f).\n", x, y);

  x = 3.0 - x;
  printf("Drugi punkt kontrolny: (%.4f, %.4f).\n\n", x, y);





  return 0;
}
