#include <stdio.h>
/* #include <math.h> */

int main() {
  /* double x = 0.16; */

  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* x = 0.17; */
  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* x = 0.18; */
  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* x = 0.19; */
  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* x = 0.21; */
  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* x = 0.22; */
  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* x = 0.23; */
  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* x = 0.24; */
  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* x = 0.26; */
  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* x = 0.27; */
  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* x = 0.28; */
  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* x = 0.29; */
  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* x = 0.31; */
  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* x = 0.32; */
  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* x = 0.33; */
  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* x = 0.34; */
  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* double x = 0.36; */

  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* x = 0.37; */
  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* x = 0.38; */
  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* x = 0.39; */
  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* x = 0.41; */
  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* x = 0.42; */
  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* x = 0.43; */
  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* x = 0.44; */
  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* x = 0.46; */
  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* x = 0.47; */
  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* x = 0.48; */
  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* x = 0.49; */
  /* printf("f(%.2f) = %.3f.\n", x, x * x * x - x * x + 2.0 * x + 1.0); */

  /* double x = -1.69; */

  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -1.68; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -1.67; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -1.66; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -1.64; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -1.63; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -1.62; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -1.61; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -1.59; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -1.58; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -1.57; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -1.56; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -1.54; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -1.53; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -1.52; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -1.51; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -1.49; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -1.48; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -1.47; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -1.46; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -1.44; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -1.43; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -1.42; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -1.41; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* double x = -0.95; */

  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -0.9; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -0.85; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -0.8; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -0.75; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -0.7; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -0.65; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -0.6; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -0.55; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -0.5; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -0.45; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -0.4; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -0.35; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -0.3; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -0.25; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -0.2; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -0.15; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -0.1; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* x = -0.05; */
  /* printf("sqrt(4.0 - (%.3f)^2) = %.4f.\n", x, sqrt(4.0 - x * x)); */

  /* double x = 0.01; */

  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.02; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.03; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.04; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.05; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.06; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.07; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.08; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.09; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.1; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.11; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.12; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.13; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.14; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.15; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.16; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.17; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.18; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.19; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.2; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.25; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.3; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.35; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.4; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.45; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.5; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.55; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.6; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.65; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.7; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.75; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.8; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.85; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.9; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.95; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 1.0; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* double x = 0.21; */

  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.22; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.23; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.24; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.26; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.27; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.28; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 0.29; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* double x = 1.05; */

  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 1.1; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 1.15; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 1.2; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 1.25; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 1.3; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 1.35; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 1.4; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 1.45; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 1.5; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 1.55; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 1.6; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 1.65; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 1.7; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 1.75; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 1.8; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 1.85; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 1.9; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 1.95; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  /* x = 2.0; */
  /* printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.1 * x / (0.01 + x * x)); */

  double x = 2.05;

  printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x,
	 0.1 * x / (0.01 + x * x));

  x = 2.1;
  printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x,
	 0.1 * x / (0.01 + x * x));

  x = 2.15;
  printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x,
	 0.1 * x / (0.01 + x * x));

  x = 2.2;
  printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x,
	 0.1 * x / (0.01 + x * x));

  x = 2.25;
  printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x,
	 0.1 * x / (0.01 + x * x));

  x = 2.3;
  printf("0.1 * (%.3f) / (0.01 + (%.3f)^2) = %.4f.\n", x, x,
	 0.1 * x / (0.01 + x * x));





  return 0;
}
