#include <stdio.h>

int main() {
  /* double x = 2.15; */

  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 2.2; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 2.25; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 2.3; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 2.35; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 2.4; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 2.45; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 2.5; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 2.55; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 2.6; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 2.65; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 2.7; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 2.75; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 2.8; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 2.85; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 2.9; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 2.95; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 3.0; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* double x = 3.05; */

  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 3.1; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 3.15; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 3.2; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 3.25; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 3.3; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 3.35; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 3.4; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 3.45; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 3.5; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 3.55; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 3.6; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 3.65; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 3.7; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 3.75; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 3.8; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 3.85; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 3.9; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 3.95; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* x = 4.0; */
  /* printf("0.5 * (%.3f) / (0.25 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 0.5 * x / (0.25 + x * x)); */

  /* double x = 0.01; */

  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.02; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.03; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */


  /* x = 0.04; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.05; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.06; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.07; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.08; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.09; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.1; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.11; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.12; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.13; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.14; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.15; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.16; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.17; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.18; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.19; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.2; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.21; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.22; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.23; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.24; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.25; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.26; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.27; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.28; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.29; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.3; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* double x = 0.35; */

  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.4; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.45; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.5; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.55; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.6; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.65; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.7; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.75; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.8; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.85; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.9; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 0.95; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 1.0; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 1.05; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 1.1; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 1.15; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 1.2; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 1.25; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 1.3; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 1.35; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 1.4; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 1.45; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 1.5; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* double x = 1.55; */

  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 1.6; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 1.65; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 1.7; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 1.75; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 1.8; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 1.85; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 1.9; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 1.95; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 2.0; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 2.05; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 2.1; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 2.15; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 2.2; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 2.25; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 2.3; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 2.35; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 2.4; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 2.45; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 2.5; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 2.55; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 2.6; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 2.65; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 2.7; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 2.75; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 2.8; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 2.85; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 2.9; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 2.95; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */

  /* x = 3.0; */
  /* printf("1.0 * (%.3f) / (1.0 + (%.3f)^2) = %.4f.\n", x, x, */
  /* 	 1.0 * x / (1.0 + x * x)); */





  return 0;
}
