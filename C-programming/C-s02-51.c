#include <stdio.h>
/* #include <math.h> */

int main() {
  /* double v = 0.86; */

  /* printf("gamma(%.3f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.87; */
  /* printf("gamma(%.3f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.88; */
  /* printf("gamma(%.3f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.89; */
  /* printf("gamma(%.3f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.90; */
  /* printf("gamma(%.3f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.91; */
  /* printf("gamma(%.3f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.92; */
  /* printf("gamma(%.3f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.93; */
  /* printf("gamma(%.3f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.94; */
  /* printf("gamma(%.3f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.95; */
  /* printf("gamma(%.3f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.96; */
  /* printf("gamma(%.3f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.97; */
  /* printf("gamma(%.3f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.98; */
  /* printf("gamma(%.3f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.99; */
  /* printf("gamma(%.3f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* double v = 0.991; */

  /* printf("gamma(%.3f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.992; */
  /* printf("gamma(%.3f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.993; */
  /* printf("gamma(%.3f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.994; */
  /* printf("gamma(%.3f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.995; */
  /* printf("gamma(%.3f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* double v = 0.996; */

  /* printf("gamma(%.3f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.997; */
  /* printf("gamma(%.3f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.998; */
  /* printf("gamma(%.3f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.999; */
  /* printf("gamma(%.3f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* double v = 0.9991; */

  /* printf("gamma(%.5f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.9992; */
  /* printf("gamma(%.5f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.9993; */
  /* printf("gamma(%.5f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.9994; */
  /* printf("gamma(%.5f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.9995; */
  /* printf("gamma(%.5f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.9996; */
  /* printf("gamma(%.5f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.9997; */
  /* printf("gamma(%.5f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.9998; */
  /* printf("gamma(%.5f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.9999; */
  /* printf("gamma(%.5f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* double v = 0.99991; */

  /* printf("gamma(%.6f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.99992; */
  /* printf("gamma(%.6f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.99993; */
  /* printf("gamma(%.6f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.99994; */
  /* printf("gamma(%.6f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* v = 0.99995; */
  /* printf("gamma(%.6f) = %.6f.\n", v, 1.0 / sqrt(1.0 - v * v)); */

  /* double x = 0.05; */
  /* double y = 0.05; */

  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 0.1; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 0.15; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 0.2; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 0.25; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 0.3; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 0.35; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 0.4; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 0.45; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 0.5; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 0.55; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 0.6; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 0.65; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 0.7; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 0.75; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 0.8; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 0.85; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 0.9; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 0.95; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 1.0; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* double x = 0.05; */
  /* double y = 1.05; */

  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 1.1; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 1.15; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 1.2; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 1.25; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 1.3; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 1.35; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 1.4; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 1.45; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 1.5; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 1.55; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 1.6; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 1.65; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 1.7; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 1.75; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 1.8; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 1.85; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 1.9; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 1.95; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 2.0; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* double x = 0.05; */
  /* double y = 2.05; */

  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 2.1; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 2.15; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 2.2; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 2.25; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 2.3; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 2.35; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 2.4; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 2.45; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 2.5; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 2.55; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 2.6; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 2.65; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 2.7; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 2.75; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 2.8; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 2.85; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 2.9; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 2.95; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 3.0; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* double x = 0.05; */
  /* double y = 3.05; */

  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 3.1; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 3.15; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 3.2; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 3.25; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 3.3; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 3.35; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 3.4; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 3.45; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 3.5; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 3.55; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 3.6; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 3.65; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 3.7; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 3.75; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 3.8; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 3.85; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 3.9; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 3.95; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 4.0; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* double x = 0.05; */
  /* double y = 4.05; */

  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 4.1; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 4.15; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 4.2; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 4.25; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 4.3; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 4.35; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 4.4; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 4.45; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 4.5; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 4.55; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 4.6; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 4.65; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 4.7; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 4.75; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 4.8; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 4.85; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 4.9; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 4.95; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  /* y = 5.0; */
  /* printf("%.3f * %.3f = %.6f.\n", x, y, x * y); */

  double x = 0.05;
  double y = 5.05;

  printf("%.3f * %.3f = %.6f.\n", x, y, x * y);

  y = 5.1;
  printf("%.3f * %.3f = %.6f.\n", x, y, x * y);

  y = 5.15;
  printf("%.3f * %.3f = %.6f.\n", x, y, x * y);

  y = 5.2;
  printf("%.3f * %.3f = %.6f.\n", x, y, x * y);

  y = 5.25;
  printf("%.3f * %.3f = %.6f.\n", x, y, x * y);

  y = 5.3;
  printf("%.3f * %.3f = %.6f.\n", x, y, x * y);

  y = 5.35;
  printf("%.3f * %.3f = %.6f.\n", x, y, x * y);

  y = 5.4;
  printf("%.3f * %.3f = %.6f.\n", x, y, x * y);

  y = 5.45;
  printf("%.3f * %.3f = %.6f.\n", x, y, x * y);

  y = 5.5;
  printf("%.3f * %.3f = %.6f.\n", x, y, x * y);

  y = 5.55;
  printf("%.3f * %.3f = %.6f.\n", x, y, x * y);

  y = 5.6;
  printf("%.3f * %.3f = %.6f.\n", x, y, x * y);

  y = 5.65;
  printf("%.3f * %.3f = %.6f.\n", x, y, x * y);

  y = 5.7;
  printf("%.3f * %.3f = %.6f.\n", x, y, x * y);

  y = 5.75;
  printf("%.3f * %.3f = %.6f.\n", x, y, x * y);

  y = 5.8;
  printf("%.3f * %.3f = %.6f.\n", x, y, x * y);

  y = 5.85;
  printf("%.3f * %.3f = %.6f.\n", x, y, x * y);

  y = 5.9;
  printf("%.3f * %.3f = %.6f.\n", x, y, x * y);

  y = 5.95;
  printf("%.3f * %.3f = %.6f.\n", x, y, x * y);

  y = 6.0;
  printf("%.3f * %.3f = %.6f.\n", x, y, x * y);

  y = 6.05;
  printf("%.3f * %.3f = %.6f.\n", x, y, x * y);

  y = 6.1;
  printf("%.3f * %.3f = %.6f.\n", x, y, x * y);





  return 0;
}
