// #include <deque>

// int main() {
//   std::deque<int>::iterator i;
//   std::deque<int>::const_iterator ci;

//   // i = ci;
//   ci = i;




//   return 0;
// }

#include <iostream>

// #include <cmath>



int main() {
  // std::cout << "sin(0.0) = " << sin(0.0) << ".\n";
  // std::cout << "sin(0.1) = " << sin(0.1) << ".\n";
  // std::cout << "sin(0.2) = " << sin(0.2) << ".\n";
  // std::cout << "sin(0.3) = " << sin(0.3) << ".\n";
  // std::cout << "sin(0.4) = " << sin(0.4) << ".\n";
  // std::cout << "sin(0.5) = " << sin(0.5) << ".\n";
  // std::cout << "sin(0.6) = " << sin(0.6) << ".\n";
  // std::cout << "sin(0.7) = " << sin(0.7) << ".\n";
  // std::cout << "sin(0.8) = " << sin(0.8) << ".\n";
  // std::cout << "sin(0.9) = " << sin(0.9) << ".\n";
  // std::cout << "sin(1.0) = " << sin(1.0) << ".\n";
  // std::cout << "sin(1.1) = " << sin(1.1) << ".\n";
  // std::cout << "sin(1.2) = " << sin(1.2) << ".\n";
  // std::cout << "sin(1.3) = " << sin(1.3) << ".\n";
  // std::cout << "sin(1.4) = " << sin(1.4) << ".\n";
  // std::cout << "sin(1.5) = " << sin(1.5) << ".\n";
  // std::cout << "sin(1.6) = " << sin(1.6) << ".\n";
  // std::cout << "sin(1.7) = " << sin(1.7) << ".\n";
  // std::cout << "sin(1.8) = " << sin(1.8) << ".\n";
  // std::cout << "sin(1.9) = " << sin(1.9) << ".\n";
  // std::cout << "sin(2.0) = " << sin(2.0) << ".\n";
  // std::cout << "sin(2.1) = " << sin(2.1) << ".\n";
  // std::cout << "sin(2.2) = " << sin(2.2) << ".\n";
  // std::cout << "sin(2.3) = " << sin(2.3) << ".\n";
  // std::cout << "sin(2.4) = " << sin(2.4) << ".\n";
  // std::cout << "sin(2.5) = " << sin(2.5) << ".\n";
  // std::cout << "sin(2.6) = " << sin(2.6) << ".\n";
  // std::cout << "sin(2.7) = " << sin(2.7) << ".\n";
  // std::cout << "sin(2.8) = " << sin(2.8) << ".\n";
  // std::cout << "sin(2.9) = " << sin(2.9) << ".\n";
  // std::cout << "sin(3.0) = " << sin(3.0) << ".\n";
  // std::cout << "sin(3.1415) = " << sin(3.1415) << ".\n";

  // std::cout << "pow(1, 1) = " << pow(1, 1) << ".\n";
  // std::cout << "pow(1, 2) = " << pow(1, 2) << ".\n";

  // std::cout << "pow(2, 0.5) = " << pow(2, 0.5) << ".\n";

  // std::cout << "pow(2, 16) = " << pow(2, 16) << ".\n";

  // double price = 4.35;
  // // double cents = 100 * price;
  // int cents = 100 * price;

  // std::cout << "cents = " << cents << ".\n";

  // int number1 = 10E100;

  // std::cout << "number1 = " << number1 << ".\n";

  // double number1 = 10E100;
  // int numberInt = static_cast<int>(number1);

  // std::cout << "number1 = " << number1 << ".\n";

  // std::cout << "numberInt = " << numberInt << ".\n";

  // int timeOfLecture = 15 + 15 + 15 + 15 + 15 + 6 + 15 + 15 + 15 + 15 + 15;

  // int bottles = 0;

  // std::cout << "Wprowadź liczbę butelek: ";
  // std::cin >> bottles;

  // std::cout << "Podana przez ciebie liczba butelek: " << bottles << ".\n";
  int int1 = 0;
  int int2 = 0;

  std::cout << "Wprowadź dwie liczby całkowite: ";

  std::cin >> int1 >> int2;

  std::cout << ".\n";

  std::cout << "Wartość int1: " << int1 << ".\n";
  std::cout << "Wartość int2: " << int2 << ".\n";





  return 0;
}
